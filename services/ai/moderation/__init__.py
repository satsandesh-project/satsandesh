"""
services/ai/moderation — the stewardship classifier (M4's lane).

Serves POST /v1/moderate against contracts/ai/moderation.py. See README.md in
this directory for how to run it and for the gating rules that turn a model's
guess into an action.
"""
