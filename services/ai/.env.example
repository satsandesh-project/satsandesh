# Copy to .env and adjust for local development. No real secrets belong here —
# Week 1 has no external services to authenticate against yet.

# Default artificial latency (ms) the mock server sleeps before responding.
# Override per-request with the X-Mock-Latency-Ms header.
MOCK_LATENCY_MS=50
