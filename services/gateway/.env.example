# Copy this file to .env and fill in real values for local development.
# .env is gitignored; .env.example is committed as the template.

APP_ENV=dev                                    # dev | staging | prod — informational, not yet branched on anywhere
PORT=8000                                       # port uvicorn listens on
DATABASE_URL=postgresql://postgres:devpass@localhost:5432/satsandesh  # matches the local `docker run` in README.md "Database"; see app/db/, alembic/
JWT_SECRET=change-me-to-a-long-random-value     # not yet used — auth is still stubbed in app/auth.py; see app/config.py
VAPID_PRIVATE_KEY=                              # generate once via `vapid --gen`, never commit the real value — see README.md "Configuration"
VAPID_PUBLIC_KEY=                               # from the same `vapid --gen` run as VAPID_PRIVATE_KEY above
VAPID_SUBJECT=mailto:you@example.com            # contact URL the VAPID spec requires in every push request's JWT `sub` claim — mailto: or https:
LOG_LEVEL=INFO                                  # DEBUG | INFO | WARNING | ERROR
CORS_ORIGINS=                                   # comma-separated list of allowed origins, e.g. http://localhost:3000,https://app.example.com
