# docs/adr/ — Architecture Decision Records

One markdown file per significant architecture decision, numbered
sequentially (`0001-...md`, `0002-...md`, ...). Never delete or rewrite
an old ADR — if a decision changes, write a new ADR that supersedes it
and link back.

Use `0000-template.md` as the starting point for new ADRs.
