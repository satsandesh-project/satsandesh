# Copy to .env for local (non-Docker) runs. In docker-compose, these are
# set directly as environment: values instead.
HOMESERVER_URL=http://localhost:8008
SERVER_NAME=localhost
AS_ID=satsandesh-circles-matrix
AS_TOKEN=changeme_generate_a_random_hex_token
HS_TOKEN=changeme_generate_a_random_hex_token
BOT_LOCALPART=satsandesh_circles_bot
BOOTSTRAP_ADMIN_USERNAME=matrix_bootstrap_admin
BOOTSTRAP_ADMIN_PASSWORD=changeme_dev_only_password
